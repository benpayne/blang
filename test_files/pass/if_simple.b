// Simple if/else statements with variable conditions

fn check( int x ) -> int
{
	return x;
}

fn run_test( int a )
{
	if a {
		check( a );
	}

	if a {
		check( a );
	}
	else {
		check( a );
	}

	if a {
		check( a );
	} else {
		check( a );
	}
}
