# Leak quarantine (test-validation U4) — MINIMAL, review-gated list of codegen
# tests with a KNOWN, tracked memory leak. Tests here still RUN under
# ./test_codegen.sh --leak-check and are reported with a distinct KNOWN-LEAK
# status, but their leak does NOT make --leak-check fatal. A leak in ANY test
# NOT listed here is fatal (teeth against new/regressed/injected leaks).
#
# All four leaks are codegen ARC gaps: refcounted temporaries allocated by
# __blang_rc_alloc (runtime/blang_runtime.c) that are never released — e.g. an
# `fn().method()` rvalue struct temporary (codegen_method_chain, pure codegen,
# no stdlib). This is a large/risky codegen-ARC fix, tracked as Open Question
# OQ-1 (overview.md) for a dedicated ARC-leak unit. Widening this list is an
# epic-scope decision, never a hire's call.
#
# Blank lines and '#' comments ignored; entries with or without .b.

codegen_method_chain.b   # 128B: fn().method() rvalue Info temporary not released (pure codegen)
codegen_file_io.b        # 232B: File/Buffer rc temporaries not released
codegen_fs_convenience.b # 335B: fs convenience-fn rc temporaries not released
codegen_http.b           # 272B: HTTP objects not released (also golden-quarantined; network)
